package pl.umkworkshop.bookstore.outgoing.descriptionStore.model;

public record DescriptionDTO(Long id,
                             String shortDescription,
                             String longDescription) {
}
